"use strict";
var assert = require('assert');

var Plane = function() {

};

describe('infinite grid', function() {
  describe('grid', function() {
    it('exists', function() {
      assert(new Plane());
    });
  });
});
